# Default values for mychart.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1

image:
  repository: ghcr.io/sonamsamdupkhangsar/user-rest-service
  tag: multitenancy
  pullPolicy: Always

imagePullSecrets:
  - name: github-regcred
nameOverride: ""
fullnameOverride: "user-rest-service"

serviceAccount:
  # Specifies whether a service account should be created
  create: true
  # Annotations to add to the service account
  annotations: {}
  # The name of the service account to use.
  # If not set and create is true, a name is generated using the fullname template
  name:

rbac:
  create: true
  rules:
    - apiGroups:
        - ""
      resources:
        - services
        - endpoints
      verbs:
        - get
        - list
        - watch

podSecurityContext: {}
# fsGroup: 2000

securityContext: {}
  # capabilities:
  #   drop:
  #   - ALL
  # readOnlyRootFilesystem: true
  # runAsNonRoot: true
# runAsUser: 1000

service:
  type: ClusterIP
#  type: NodePort
  port: 80
  targetPort: 8080

istio:
  enabled: false

ingress:
  enabled: false

resources:
  requests:
    cpu: 150m
    memory: 512Mi
  limits:
    cpu: 500m
    memory: 768Mi

#nodeSelector:
#  node.kubernetes.io/instance-type: s-1vcpu-2gb

affinity: {}

postgres:
  port: "5432"
  service: user-db-rw
  secretfile: user-db-app
  dbname: user

envs:
  - name: JAVA_TOOL_OPTIONS
    value: "-XX:MaxRAMPercentage=70 -XX:InitialRAMPercentage=25"
  - name: DB_SSLMODE
    value: REQUIRE
  - name: SPRING_PROFILES_ACTIVE
    value: kubernetes
  - name: SPRING_CLOUD_KUBERNETES_DISCOVERY_NAMESPACES_0
    value: "{{ .Release.Namespace }}"
  - name: AUTHENTICATION_REST_SERVICE
    value: "http://authentication-rest-service.{{ .Release.Namespace }}.svc.cluster.local"
  - name: ACCOUNT_REST_SERVICE
    value: "http://account-rest-service.{{ .Release.Namespace }}.svc.cluster.local"
  - name: ORGANIZATION_REST_SERVICE
    value: "http://organization-rest-service.{{ .Release.Namespace }}.svc.cluster.local"
  - name: ROLE_REST_SERVICE
    value: "http://role-rest-service.{{ .Release.Namespace }}.svc.cluster.local"
  - name: AUTH_SERVER_ROOT
    value: "http://authorization-server.{{ .Release.Namespace }}.svc.cluster.local"
  - name: ISSUER_URI
    value: https://platform.openissuer.com/issuer
  - name: JWT_SET_URI
    value: https://platform.openissuer.com/issuer/oauth2/jwks
  - name: OAUTH_AUDIENCES
    value: oauth-client,nextjs-client
  - name: NEW_RELIC_ENABLED
    value: false

health:
  liveness:
    path: /users/api/health/liveness
    port: 8080
    initialDelaySeconds: 30
    periodSeconds: 5
    failureThreshold: 5
  readiness:
    path: /users/api/health/readiness
    port: 8080
    initialDelaySeconds: 30
    periodSeconds: 5
    failureThreshold: 5

secretFiles:
  - file: service-service-client-credential-flow-secret
    keys:
      - key: base64EncodedClientIdSecret
        name: BASE64_CLIENT_ID_SECRET
      - key: scopes
        name: REQUEST_SCOPES
