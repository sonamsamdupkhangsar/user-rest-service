CREATE TABLE if not exists My_User (id UUID PRIMARY KEY, first_name varchar, last_name varchar, email varchar,
  authentication_id varchar, birth_date timestamp, profile_photo varchar, gender_id UUID);